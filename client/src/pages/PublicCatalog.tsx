import { Link } from "wouter";
import { ArrowLeft, ArrowUpRight, Headphones, LockKeyhole, Music2, ShoppingBag } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";

function money(cents = 0) {
  return new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 0 }).format(cents / 100);
}

export default function PublicCatalog() {
  const beats = trpc.catalog.publicList.useQuery();
  return <div className="min-h-screen bg-[#070b0a] text-[#e8f3ed]"><div className="pointer-events-none fixed inset-0 opacity-40 [background-image:radial-gradient(circle_at_80%_20%,rgba(16,185,129,.16),transparent_26%)]" /><header className="relative flex items-center justify-between border-b border-white/[.07] px-5 py-5 md:px-12"><Link href="/" className="flex items-center gap-3 text-sm text-slate-300 hover:text-emerald-300"><ArrowLeft size={17} /> Duck Hub</Link><div className="flex items-center gap-2 text-sm text-slate-500"><ShoppingBag size={16} /> Loja de Beats</div></header><main className="relative mx-auto max-w-6xl px-5 py-14 md:px-12"><div className="max-w-2xl"><Badge className="border-emerald-400/20 bg-emerald-400/10 text-emerald-300">DUCK / CATALOG</Badge><h1 className="mt-5 text-4xl font-semibold tracking-tight md:text-6xl">Instrumentais para desbloquear sua próxima <span className="text-emerald-300">versão.</span></h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-400">Ouça previews com watermark, escolha uma licença e receba seu contrato e arquivos com entrega segura.</p></div><div className="mt-14 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{beats.data?.length ? beats.data.map(beat => <Card key={beat.id} className="overflow-hidden border-white/[.08] bg-[#0b1210]/90"><div className="flex aspect-[1.55] items-center justify-center bg-gradient-to-br from-emerald-950 to-[#101a16] text-emerald-300"><div className="grid h-16 w-16 place-items-center rounded-full border border-emerald-300/30 bg-emerald-300/10"><Headphones size={24} /></div></div><CardContent className="p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="font-medium">{beat.title}</h2><p className="mt-1 text-sm text-slate-500">{beat.genre || "Instrumental"} · {beat.bpm || "—"} BPM {beat.musicalKey ? `· ${beat.musicalKey}` : ""}</p></div><Badge variant="outline" className="border-emerald-400/20 text-emerald-300">{beat.availability === "available" ? "Disponível" : "Reservado"}</Badge></div><div className="mt-5 flex items-end justify-between"><div><div className="text-xs text-slate-600">Não exclusiva a partir de</div><div className="text-lg font-semibold text-emerald-300">{money(beat.nonExclusivePriceCents)}</div></div><Button className="bg-emerald-400 text-[#06120d] hover:bg-emerald-300">Escolher licença <ArrowUpRight size={15} /></Button></div></CardContent></Card>) : <div className="col-span-full rounded-3xl border border-dashed border-white/10 bg-white/[.02] p-12 text-center"><Music2 className="mx-auto text-emerald-300" size={26} /><h2 className="mt-4 text-lg font-medium">O catálogo está pronto para o seu primeiro lançamento</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Ainda não há beats publicados. Quando você adicionar um, ele aparecerá aqui com licenças e preços.</p></div>}</div><div className="mt-12 flex items-center gap-3 rounded-2xl border border-white/[.07] bg-white/[.025] p-5 text-sm text-slate-400"><LockKeyhole size={18} className="text-emerald-300" /> Os downloads privados são entregues por URLs assinadas com expiração curta após a confirmação do pagamento.</div></main></div>;
}
